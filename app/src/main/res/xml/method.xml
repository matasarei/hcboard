<?xml version="1.0" encoding="utf-8"?>
<!--
    No subtypes on purpose: languages live in the keyboard's own settings and on its globe key.
    Declaring them here too gave Android a second list, filled from the phone's languages, that
    the keyboard's settings did not agree with, and a picker whose choice never reached the keys.
-->
<input-method xmlns:android="http://schemas.android.com/apk/res/android"
    android:settingsActivity="net.matasar.keyboard.settings.SettingsActivity"
    android:supportsInlineSuggestions="true"
    android:supportsSwitchingToNextInputMethod="true" />
